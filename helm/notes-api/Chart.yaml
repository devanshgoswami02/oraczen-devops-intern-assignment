apiVersion: v2
name: notes-api
description: Notes API (FastAPI + PostgreSQL) — Oraczen DevOps take-home assignment
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: ">=15.0.0"
    repository: "oci://registry-1.docker.io/bitnamicharts"
    condition: postgresql.enabled
