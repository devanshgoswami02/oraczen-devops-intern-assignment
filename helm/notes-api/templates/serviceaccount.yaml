{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "notes-api.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "notes-api.labels" . | nindent 4 }}
{{- end }}

