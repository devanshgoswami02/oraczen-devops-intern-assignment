Put your GitHub Actions workflow(s) here (e.g. `ci.yaml`). See `ASSIGNMENT.md` (Part 3) for requirements.

This README is a placeholder only — delete it once you add a real workflow file, GitHub Actions won't pick up a workflow unless it's YAML directly under this folder.
